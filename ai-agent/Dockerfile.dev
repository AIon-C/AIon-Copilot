FROM node:20-slim

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

COPY tsconfig.json ./
COPY src/ ./src/

EXPOSE 3001
ENV NODE_ENV=development
ENV PORT=3001

CMD ["npx", "tsx", "watch", "src/index.ts"]
