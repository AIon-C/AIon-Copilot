apiVersion: v1
kind: Service
metadata:
  name: ai-agent
  labels:
    app: ai-agent
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  selector:
    app: ai-agent
