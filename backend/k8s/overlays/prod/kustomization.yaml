apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

replicas:
  - name: backend
    count: 2

images:
  - name: backend
    newName: asia-northeast1-docker.pkg.dev/aion-copilot/aion-copilot/backend

patches:
  - target:
      kind: Deployment
      name: backend
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: backend
      spec:
        template:
          spec:
            containers:
              - name: backend
                imagePullPolicy: IfNotPresent
  - target:
      kind: ConfigMap
      name: backend-config
    patch: |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: backend-config
      data:
        GCS_ENDPOINT: ""
        GCS_PUBLIC_URL: ""
        REDIS_URL: "redis://redis:6379"
  - target:
      kind: Secret
      name: backend-secret
    patch: |
      apiVersion: v1
      kind: Secret
      metadata:
        name: backend-secret
      stringData:
        DATABASE_URL: "REPLACE_WITH_CLOUD_SQL_URL"
        JWT_SECRET: "REPLACE_WITH_PRODUCTION_SECRET"
